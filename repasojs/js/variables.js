//Cear variables forma antigua de crear variables
//Por defecto una variable sin valor arroja undefined
var aprendiendo = true;
aprendiendo = false; // var se puede reescribir sin dar error

/**
 * Crear constante con const
 * No se puede inicializar vacio ya que debe constar de un valor al iniciar
 * Se usa generalmente para seleccionar elementos de la interfaz del sitio web
 */
const aprendiendo = 'Javascript';


/**
 * Let es la nueva forma de declarar variables, funciona como la palabra reservada var,
 * solo que let no permite la redeclaracion de la misma y es algo mas estricta que var
 * @type {string}
 */
let aprende = 'Javascript';